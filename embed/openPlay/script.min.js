document.addEventListener("DOMContentLoaded", () => {
const player = new OpenPlayerJS("video", null, false, {
  controls: {
    left: ["play", "time", "volume"],
    middle: ["progress"],
    right: ["levels", "settings", "fullscreen"],
     }, 
    forceNative: true,
    });
  var maskPrx = "https://worldradio.online/proxy/?q=";
player.src = { 
    type: "application/x-mpegURL", 
    src: "//worldradio.online/proxy/?q=" + sURL
   };
  player.init();
  player.getElement().addEventListener('hlsLevelLoaded', (e, data) => {
  console.log(e, data);
})
  });
